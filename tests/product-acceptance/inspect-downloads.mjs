import {readFile,readdir} from 'node:fs/promises';import {join} from 'node:path';import {readDownloadZip} from './readback.mjs';import {sha256} from './artifact.mjs';
for(const name of await readdir(process.argv[2]))if(name.endsWith('.zip')){try{const b=await readFile(join(process.argv[2],name)),files=readDownloadZip(b);console.log(JSON.stringify({name,bytes:b.length,hash:sha256(b),files:[...files].map(([path,bytes])=>({path,bytes:bytes.length,sha256:sha256(bytes)})),packageJSON:files.has('package.json')?JSON.parse(files.get('package.json').toString()):null}));}catch(e){console.log(JSON.stringify({name,error:e.stack}));}}