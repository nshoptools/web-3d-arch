# Analytic adjudication before changing fixture expectations (2026-09-08)

The immutable preimage keeps the entire old fixture and run.mjs. Exact old requests remain R2 negative reproducers. B(0)=0; B(n)=h0+(n-1)h. A tag and reference must name the same manufacturing face.

- Old first-layers, relative-layers and rim-layers: body top nominal2.4, h0=.27 and h=.16/.20/.25. B(0)=0 and B(2)=.27+h do not equal2.4; these schedules have no boundary at2.4. Ref0/ref2 on art.bottom/rim.bottom is invalid.
- Old flat-layers (phang2): flat.bottom2.4, ref3 implies .27+2h, invalid.
- Old recess-layers: recess.top2.4, ref0 implies bed0; depth must count downward from the actual TOP boundary.
- Old cap-layers: same cap.top/ref1=.27 applied to different column tops3.2 and4.0; neither is .27. A single explicit reference cannot bind two unequal top faces.
- Old text MM defaults declare tags133/134 with ref0. Model base starts3.2 (4.0 with bands), glyph3.8 (4.6 with bands). Detached base starts0 but glyph.6. At least one declared reference is invalid in every such case. Positive nominal MM tests must explicitly use the established unspecified datum0/ref0 convention; old declarations remain negative requests.
- Old text-layers h0=.27 declares base ref0 on3.2 and glyph ref3 on3.67. B(0)=0, B(3)=.67: both wrong.

These are binding errors, not new geometry preferences. Corrected positive requests are separate and use analytically consistent explicit boundaries or unspecified MM. Product code never changes the caller record. Narrow charm rectangles have an independent pre-existing wall failure; unchanged old/new outcomes are reported separately, never counted as successful geometry.
