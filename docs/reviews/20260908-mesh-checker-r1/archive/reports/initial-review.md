**Independent mesh qualification and final-scene authority review — 20260908-mesh-checker-review-r1**

No substantiated defect was found in the reviewed mesh qualification and final-scene authority paths. No reproduced case published a cached pass for a hidden, retired, changed, or incorrectly mapped mesh. This is a bounded component review; it does not qualify the whole application or establish readiness to print.

This initial conclusion was independently sealed before consulting any author or other reviewer's conclusions. No such conclusions were consulted. Only frozen product bytes in `work/review-source` were reviewed. No product code, captured source, other room, or shared toolchain was changed. All generated evidence and private browser profiles remain in this run's six permitted directories.

The actual selected session metadata reports `gpt-6-astra`, effort `max`, CLI `0.153.4`; the CLI executable SHA-256 matches the launch pin. The launch uses `--ignore-user-config`, `service_tier="default"`, and `features.fast_mode=false`; the effective feature listing also reports Fast off. The selected metadata and launch evidence are in [seat-attestation.json](../evidence/seat-attestation.json). On 2026-09-08 I fetched the official [model documentation](https://learn.chatgpt.com/docs/models), [speed controls](https://learn.chatgpt.com/docs/agent-configuration/speed), and [configuration reference](https://learn.chatgpt.com/docs/config-file/config-reference). Astra/max remains available in the selected local model catalog. The documented Ultra option involves automatic delegation; this review expressly prohibits subagents and mandates max. No stronger-name inference, downgrade, or configuration change was made.

The source manifest SHA-256 is `09091bbc31f6216151c869ef75650adbdd3b1ce18b998ea1e74deab05568771c`. All **702 files** matched their recorded byte lengths and hashes before and after the review; the final snapshot inventory contained no extra files. See [before](../evidence/source-hashes-before.json) and [after](../evidence/source-hashes-after.json). The runtime was copied byte-for-byte from captured `runtime-production` into this run's `work/module`. Its pins are:

| Captured runtime file | Bytes | SHA-256 |
| --- | ---: | --- |
| `arch-kernel.mjs` | 138932 | `950225880c23476f9a6523111ac5a33aebb24f1d89bfa1d8f60bff1b1cc1c375` |
| `arch-kernel.wasm` | 5752782 | `211b392f097eb55c8b52898e7b6123cb4b4b4539bfc68a5223d2f9e4c4d0e4cd` |

The production pair was **not independently rebuilt**. Hash matching and observed behavior do not prove that the captured native source produced that binary. Runtime provenance, a second real mixed binary pair, and general native union equivalence remain unqualified.

The governing requirements were captured OUT-01 (`docs/specs/03-ky-thuat.md:291`), ABI-01 (`:172`), WEB-01 (`:226`), GEO-01/02, EXP-02 (`docs/specs/01-chuc-nang.md:249`), and `docs/assets/INPUT-CONTRACT.md`. In particular, edge incidence alone and native success flags cannot establish manifoldness; material interiors and the whole union require examination; invalid or stale state must block; resource exhaustion is unverified. The requested name `scene-material-ledger.mjs` does not exist in the manifest. I reviewed the actual `src/integration/material-source-ledger.mjs` and the currently wired `sceneMaterialBindings` in `product-services.mjs:16`.

The mathematical review covered `mesh-predicates.mjs:15` (binary64-to-integer conversion), `:59` (triangle intersection), `:73` (ray parity), and `mesh-qualification.mjs:24` (floating filter), `:67` (topology), `:89` (embedding), and `:123` (containment). Coordinate welding is exact. Homogeneous intersection comparisons preserve denominator sign. Noncoplanar triangles sharing an edge can intersect only on that edge; coplanar neighbors still take the exact intersection path. The floating filter's generous error multiplier and small-value fallback were examined under the transport coordinate bound. This reasoning and the differential tests provide evidence, not a formal proof for every binary64 input.

Independent checks and their limits are recorded separately from supplied framework tests:

| Independent check | Observed result | Reproducer / evidence |
| --- | --- | --- |
| Analytical meshes, transport and budgets | 31 checks matched expected results: valid solids, open/inverted/duplicate/degenerate faces, pinched vertex links, crossings, cavities and islands, material overlap, shared faces, edge/point-only contacts, tiny gaps, subnormal coordinates, offset views, NaN, truncation, plain arrays, cancellation and lowered budgets | `work/analytic-shapes.mjs`, `work/independent-geometry.mjs`; `evidence/independent-geometry-results.json` |
| Seeded box interior oracle | 300 interval-volume comparisons; no positive verdict with analytical interior overlap | Same geometry reproducer and evidence |
| Exact rational triangle oracle | 2,909 nondegenerate pairs; 1,577 intersecting; no disagreement and no unsafe floating exclusion. Includes 360 shared-vertex, 358 shared-edge, 344 coplanar, 483 near-coplanar, and 647 dynamic-range pairs | `work/predicate-corpus.mjs`, `work/predicate-oracle.py`; `evidence/predicate-corpus.json`, `evidence/predicate-oracle.json` |
| Convex solid oracle | 5,000 integer tetrahedron pairs; all 2,971 pairs with strictly overlapping interiors rejected; 556 unresolved contact cases stayed unverified | `work/adversarial-solids.mjs`; `evidence/adversarial-solids.json` |
| Concave solid oracle | 450 generated polycube pairs; no positive verdict for any of the 144 pairs sharing occupied unit cells. Nonmanifold generated surfaces are allowed to fail | `work/voxel-oracle.mjs`; `evidence/voxel-oracle.json` |
| Containment and adjacent-face fold | 2,197 exact interval classifications of a box with a cavity agreed. A closed, connected octahedral surface with coplanar overlapping faces sharing two vertices failed `MESH_SELF_INTERSECTION` | `work/contact-and-containment.mjs`; `evidence/contact-and-containment.json` |
| Provider authority and current material callback | 28 independently authored boundary checks: complete/unused/full material identities, hidden model, epoch/lease retirement, duplicate IDs/indexes, upstream blocking, missing capabilities/methods, wrong readback hashes/generation/membership/source/key, state/session/byte/metadata/cancel races, observed A-B-A, and reordered material tables | `work/independent-authority.mjs`, `work/independent-authority-launch-v2.mjs`; `evidence/independent-authority.json` |

The rational oracle uses Python `Fraction.from_float` and convex separating axes, rather than the production triangle-cut implementation. The tetrahedron oracle uses bounded integer separating axes; the polycube oracle counts intersecting occupied cells. Finite seeds are recorded in each reproducer and result. These samples are not independent application use cases and are not an exhaustive domain proof.

The provider boundary rig uses explicit synthetic root transport and inspector doubles, with the real qualifier and actual `sceneMaterialBindings` function. It does not treat fabricated inspector records as production authority. Production inspection additionally validates the APMS descriptor/request hash and registered model in `product-adapters.mjs:358` and `:390`. The provider checks the visible model, current state stamp/head, session, root bytes, metadata, epoch and mapping at asynchronous boundaries (`final-scene-evidence.mjs:84`, `:91`, `:111`). Material readbacks validate complete original membership and full keys (`:53`), before a separate neutral analytical union. That neutral key did not enter the real material table or overwrite model stats. `status: ready` with `meshVerdict: unverified` was observed for missing union capability and was correctly distinguished from a pass. The actual gate checks bind inspected head/source/assembly/schedule data (`final-scene-gates.mjs`); unknown or blocked native state was rejected.

The real root and real independent validator Worker were exercised in **Chromium 153.0.8010.12, Firefox 155.0, and WebKit 26.6**, with the captured production pair. The independently authored browser sequence ran the 22 analytical geometry cases, validator cancellation/reset/watchdog/stale-message checks, then generated a 20 × 10 × 2 mm adjacent-rectangle source through the actual root. AFGM preserved two full material identities even when slot and RGBA were equal; same-material and neutral analytical grouping produced one union. All four readbacks measured 400 mm³ using an independent determinant-volume sum and passed the checker. Complete binary64 `.arch` readbacks and metadata are retained, not just native labels. Invalid native membership/source/revision requests were rejected; cancelling a dispatched AFGM operation returned `CANCELLED`; source bytes stayed unchanged and calling release twice sent one source release.

Successful independent browser evidence is in `evidence/independent-3/chromium.json`, `evidence/independent-3/webkit.json`, and `evidence/ind-ff-4/firefox.json`, with sibling `.arch` files. Reproducers are `work/independent-browser-page-v3.mjs`, `work/independent-browser-v3.mjs`, and `work/independent-browser-v4.mjs`. The 100 ms watchdog test deliberately uses an idle reviewer Worker and the supported shorter watchdog setting; normal product operations retained default timeouts. Every browser server used explicit loopback routes, CSP, COOP/COEP/CORP, disabled service workers, and private profiles. No backend, repository metadata, review state or credentials were served.

Separately, the supplied Node suite passed all 43 framework tests, and the adapted supplied browser suite completed the five generated product requests (keychain, clicky, strap, lego, charm) in all three engines. These are five supplied requests exercised across engines, not 15 independently sourced geometry truths. Its browser material callback uses the persisted ledger; the application callback was exercised separately in the independent Node boundary rig. The complete compiled application composition/UI was not run. Supplied checks are recorded in `evidence/supplied-node-attempt-1.log` and `evidence/browser-baseline-1/`; their expectations did not determine this conclusion.

Failed setup and earlier attempts remain preserved. The first independent browser route collector encountered an optional uncaptured text-renderer path and stopped before launch. Later collection omitted that unserved dependency. The original mm-unit SVG test's nominal-volume assertion was too strict for the source representation: a separate retained diagnostic measured X extent 20.000001 mm and volume 400.00001999999995 mm³ **both before and after AFGM**, with zero source-to-AFGM difference. The final analytical request used explicit size and integer viewport coordinates and retained its exact 400 mm³ expectation; no checker tolerance was changed. Firefox navigation timed out in two independent harness attempts. A shorter private profile and a new persistent-context page succeeded with the same security headers and timeouts; the precise cause was not isolated. The Node authority rig initially lacked a bare-package resolver, then its first resolver hook recursed. The corrected launcher resolves only already pinned `fflate` and `@xmldom/xmldom` paths before registering the hook. These are harness failures, not product defect findings or successful product scenarios.

Limits remain material. I did not run a native rebuild, real alternate/mixed binary pair, complete UI/build/typecheck, 400k/600k-triangle worst-case or maximum-memory campaign, exhaustive ray-ambiguity/contact arrangements, general semantic correctness of product geometry, all source formats, final serialized export validation, slicers, printers, coupons or physical fit. Model/source/assembly inspection tests establish authority behavior, not that every mechanical parameter produces the intended solid. The checker explicitly leaves thin features, design intent, global pipeline error, fit and slicer behavior unverified/unqualified; `final-scene-evidence.mjs:204` also leaves union geometry equivalence unverified. Those fields must remain visible to downstream qualification and cannot be promoted to whole-product readiness. Within the ordinary analytical and supplied default inputs exercised here, I found no excessive refusal that substantiates a functional defect.

All findings are therefore represented by an empty findings array in [initial-findings.json](initial-findings.json). Commands, actual exit codes, pinned versions, failed attempts, all source pins, evidence/reproducer hashes and the initial seal are indexed by the final evidence manifest. No implementation was performed.
