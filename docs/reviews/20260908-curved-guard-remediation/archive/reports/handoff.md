# Bounded curved-guard remediation handoff

Frozen implementation/self-test candidate; no qualified independent-review claim.

Promote only the 17 entries in reports/checked-files.json: two CPP replacements and 15 new tests/docs. Do not copy the surrounding captured source tree. Original preimages are in preimages/. At seal main already matches both announced candidate CPP hashes, rather than the old preimages: parent began promotion from the early source pins. The first seal attempt refused that state; this seal records both states explicitly. Captured old preimages and own unchanged source_assembly.cpp are verified. Main source_assembly.cpp advanced separately during parent source promotion; its new hash is recorded in authoritative-results and was not compiled in this own candidate. Parent combined rebuild remains required. No main write by this worker.

F1 and F2 independently reproduced on the canonical locked Clipper tree6316dc (not the reviewer's alternate dependency tree). Four unsafe acceptances plus three pre-existing negative controls reproduced on both actual prepared-source child targets. Final seven cases return typed refusal with blocked export, no published mesh, unchanged source/request/parameters.

F1 keeps the raw unsimplified positive residual branch first. Only unresolved contact fallback is admitted by bounded referenced-ring separation across source slabs and distinct Z levels. The oblique fixture has 2nm axial shift but about0.05nm perpendicular width; the segment-distance gate catches it. The existing fallback remains conditionally numerical, never an all-input full-volume proof. Positive roof below the same slit passes without source-resolution cleanup; intact split slabs also pass. F2 checks local parallel/near-parallel footprint edges before the legacy mouth scan can skip them. No manufacturing geometry, source bytes or tolerance settings are changed.

## Executed final evidence

- Native and WASM: strict original7/7, adjacent/positive controls10/10, unchanged core40/40 builds and40/40 serialized-mesh numeric oracles each.
- Native in-flight cancellation:3 observers pass, no partial mesh.
- Supplementary existing R2 guards:18/22 assertion passes EACH target, exit1 retained. Two gap negatives still refuse but report an earlier typed diagnostic. The central4x4mm hole tangent to radius2 bore and detached2x2mm island with walls y±1 inside the bore band are now conservatively unsupported. No claim those prior meshes are unsafe; no expected assertion was lowered or removed.
- 12 variants, full resource/child matrix and parent root/browser integration deferred under the urgent bounded handover instruction. No native-WASM byte/surface parity claim beyond both independent target oracles in this run.

Authoritative counts, report hashes and final binary pins: reports/authoritative-results.json. Exact compiler commands/exits: evidence/build-{native,wasm}-bounded-r5-commands.json and receipts/logs. Test commands: tests/curved-guards/README.md; core per-case command arrays: evidence/core-{native,wasm}-r5/commands.json. Both initial oracle invocations missed CURVED_CORPUS and failed ENOENT; failed reports/logs remain, then only the oracle was rerun with CURVED_CORPUS=corpus-production (exit0). Source stage and derive ran once. All exploratory failures remain evidence, not release qualification.

## Limits and integration

See candidate docs/curved-products/GUARD-RESOLUTION.md for exact thresholds,20M pair work cap, typed refusals, shared-interface exceptions and remaining cavity wall/opening backend policy. The admission restriction is not a proof for all CSG-created fine features or arbitrary junction arrangements. Child ABI2 and metadata layouts unchanged; diag111/112/113 explain conditional qualification. Actual application production of the adversarial slab requests remains unproven. Preserve old snapshots/source and handle AM_INVALID/exportBlocked without partial publication. Parent owns promotion and unified production rebuild.

No Opus/other seat was invoked; main/toolchain/prior/reviewer rooms remained read-only.
