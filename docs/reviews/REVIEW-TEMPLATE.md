# Review: <chủ đề>

- Ghế / mã phiên:
- Ngày/phiên bản [cấu hình ghế](seat-config.json) đã áp dụng:
- Công cụ / phiên bản; cách gọi hoặc tiếp tục phiên:
- Model yêu cầu / model thực tế (model đích nếu dùng alias):
- Effort / chế độ thực tế (mọi lượt Opus hiện hành phải dùng Max, Ultracode tắt):
- Fast mode thực tế (Codex/Opus phải tắt theo cấu hình hiện hành):
- Bằng chứng xác minh cấu hình (đường dẫn trong phòng review / metadata phiên):
- Kiểm tra lựa chọn mới mạnh hơn: ngày, nguồn, kết quả / điều chưa xác minh:
- Trạng thái cấu hình: đã xác minh / không đáp ứng / chưa xác minh; lý do:
- Phạm vi và revision/hash đầu vào:
- Thời gian:
- Đọc kết luận ghế khác trước khi review: không / giải thích nếu có

## Phát hiện

Với mỗi vấn đề: mức độ, đường dẫn và dòng hoặc JSON id, điều kiện tái hiện,
tác động, bằng chứng tương đối trong phòng review, đề xuất sửa.
Nếu không có phát hiện, ghi rõ phạm vi đã kiểm tra và phần chưa kiểm tra.

## Kiểm chứng

| Lệnh | Exit code | Bằng chứng | Kết quả |
| --- | --- | --- | --- |

## Kết luận

Đạt / cần sửa / chưa đủ bằng chứng. Ghi giới hạn kiểm tra và rủi ro còn lại.
