# Checked current-main export integration

Run `20260908-export-app-root-integration`, candidate `work/export-app`.
This rebases executable float28 (manifest
7b7b1f21bc9b8a171e963ab5a0045c1e687fd1f955e3867b8994719bb74d57b8) onto
captured current main. Root, earlier rooms and .toolchain were read-only.
Latest owner instruction disables every Opus invocation; none was used.
Verification is implementation work, not configured independent review.

## Delta ownership

Only three production files change: `src/integration/export-adapters.mjs`, its
`.d.mts`, and one outer-catch hunk in `src/core/engine-worker.mjs`. Current
finalScene/product ownership, application jobs/configuration/receipts, profiles,
source/viewport/printing providers, runtime integrity, source frame, CSG and native
mechanics/source children are preserved. Earlier `createProductExportEvidence`
is not restored. Constructor and parent public prepared-proposal contract stay
unchanged. `root-hooks.json` records exact before/after SHA; apply only the
focused production patch. New tests live in `tests/export-root-integration/`.
All existing parent integration tests and legacy runner bytes are preserved.
For raw-byte parity on Windows, use command-local Git options
`-c core.autocrlf=false -c core.eol=lf` when applying the patch. The three
patched private preimages then match the tested candidate byte-for-byte. No
repository or machine Git setting is changed; see reports/patch-check-lf.json.

Two bounded improvements beyond the frozen executable are included: full message
equality limits diagnostic routing to the two documented suffixes; original input
and confirmation signal references prevent listener leaks after caller property
replacement. Both signal regressions fail on captured pre-fix code and pass after
the change. They also run through actual RPC with no late file/receipt publication.

## Evidence

102 Node checks = current ordinary53 + float lifecycle28 (labelled doubles) +
current provider20 + exact Worker whitelist1. This replaces the obsolete21
frozen evidence-factory tests with the current20 provider tests; it is not a claim
that the historical98 suite ran unchanged. Current parent proposal/receipt8,
actual same-Module helper WASM8 and strict types all pass. Three engines each
pass12 actual app/root RPC cases: Chrome, Firefox and WebKit. They use real
product inspection, gates, full material ledger, AFGM and mesh-check Worker;
injectedMeshQualification=false. Only fixture source orchestration is a callback
over real owned SVG contexts. Four serialized files have byte-exact browser parity.

Every browser fetches WASM once through verified-owned-wasmBinary. Exact pair:
MJS64c5f89e2ee8fecda371436f907faf83e9120c41e11089979bd011bc8a197ed2,
WASMf14689439108be1abfd6db77c06d758e08b172980313efe905d3a6d6d196287e.
Parent build receipt SHA f18343cea5f75cd8fe12a2e33f7052695a15b20ab4af7e0664e9b4a8ba470cb3
was captured as input, not claimed as an independently rerun native build. Earlier
b67d pins remain historical inputs; all checked acceptance uses f146.

Original fixture-only native exports do not exist in the production pair. The
portable ordinary tests now construct canonical SVG paint slabs through the
production API. Their 2D paint overlap is resolved upstream: they do not claim to
replace final-child intersecting-solid union tests. The section fixture is an
explicit through-opening. Real source/viewport UI and target slicer/physical fit
are outside this integration evidence. Conditioned output stays unverified;
original source proof is never relabelled as output proof. Curved guard findings
are assigned to Boole and are not qualified or repaired by this export delivery.

## Portable replay

From main after applying deltas, or from the supplied candidate, invoke its
`tools/export-app/root-run.ps1`. Supply a fresh own RunId, explicit local ModulePath,
ExpectedMjsSha, ExpectedWasmSha and -Browsers. Example arguments for this pair:

`-ExpectedMjsSha 64c5f89e2ee8fecda371436f907faf83e9120c41e11089979bd011bc8a197ed2
-ExpectedWasmSha f14689439108be1abfd6db77c06d758e08b172980313efe905d3a6d6d196287e`.

The script finds root by tools/project-env.ps1, dots the codex seat environment,
sets own Cargo/cache/temp, copies a hashed JS/test/dependency closure and the exact
pair into the run, and runs Node/types/controls/helper plus optional three browsers.
No downloads/build/install occurs. -CaptureTag permits another immutable capture
inside the same unfrozen run. Changed captured inputs or runtime pins fail closed.
The complete script was exercised with -CaptureTag checked -Browsers. Its results
are in evidence/root-integration-checked and evidence/root-browser-checked.
Use a fresh run after freeze. A replacement Module pair needs its own rerun.
