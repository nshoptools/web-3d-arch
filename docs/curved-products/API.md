# Integration contract — curved product child delta

No public C/C++ or Rust layout changes. Mechanics ABI 2 / semantics 3,
source ABI 1 / semantics 2 and datum extension 1 remain the preimages.
The existing source request, parameter origins, layer bindings, source/material
IDs and source borrowing contract are unchanged. No new context pointer,
generation allocator, decoder, registry or second WASM instance is introduced.

The parent continues its current product bridge:

```text
owned root canonical source snapshot + captured APRQ
  -> existing root native product-bridge request construction
  -> arch_source_assembly (integer source material partition)
  -> borrowed ArchMechSource2 / existing mechanics request
  -> arch_mechanics (bounded groove / mouth, final solid guards)
  -> existing APMS semantics + ARCH1 manufacturing snapshot
  -> root lease / current-head / export authority checks
```

Copy only the manifest's four modified files: three production child sources
and one synthetic fixture. New `tests/curved-products`, `tools/curved-products`
and `docs/curved-products` are portable replay/documentation additions. All other
candidate files are read-only borrowed preimages or private build scaffolding.
In particular, do not copy the candidate root CMake file or native bridge copies
over main. The exact source SHA preconditions are in `reports/delta.json`.

No root hunks are proposed. Keep the parent's ASFR canonicalization, runtime
integrity initialization, imported CSG, root generation/lease ownership and
export gates. Halley's source context/palette construction remains authoritative.
New native builds must link the changed child objects into the parent's existing
Module. The standalone replay target intentionally excludes Rust registry and
browser RPC: component evidence cannot qualify a whole application or export.

Typed failures remain failures with zero published mesh and unchanged caller
input. Resource diagnostics identify actual scanned/constructed work; requested
nominal dimensions, error budgets and export gates are not changed to avoid a
refusal. Manufacturing coordinates and preview transforms remain separate.

The captured real-source fixtures are bound to production pair `95022588…` /
`211b392f…`; all 120 source/state/request recapture files matched their originals.
The parent's newer `module-source-r1` pair is outside this candidate's runtime
qualification. Tests replay those immutable ARCH1/APRQ bytes through candidate
children, not an invented product snapshot or a preview bitmap.
