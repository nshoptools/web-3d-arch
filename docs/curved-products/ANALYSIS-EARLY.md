# PSB-03 curved products — implementation delta, 2026-09-08

Scope: only mechanics/source-assembly child modules and portable fixtures.
Root registry, runtime/Worker, exports, original/derived libraries and frozen
rooms are read-only. No API change proposed. Current ABI 2/1 and semantics 3/2
remain the integration preimages; final manifest will identify exact changes.
This is implementation work, not a configured independent review.

Native replay of the exact 40 captured Inter O / selected Noto monochrome 😀
project states reproduces all 22 PSB-03 refusals, with 18 published models.
The committed outlined SVG assets are borrowed, not reshaped. Current root
product source contexts and adapter pack actual APRQ plus canonical ARCH1
buffers; an unchanged copy of root product-bridge.cpp calls candidate children.
There is no second source decoder, boolean or triangulator.

| Finding | Measured evidence | Bounded implementation direction |
|---|---|---|
| Chìm, all five products | Bottom partition directed boundary cancellation leaves 4–42 residual edges. Example emoji/charm has −1.440735e−7 mm² signed area difference and 12 residual edges; pairs differ by exactly one integer nm. | Fix the shared material partition construction, not the bottom support guard. Quantized artwork and its complement must derive from the same integer boundary. Check every Z interval, including upper flat material seams. |
| Strap | Text footprint 581 edges, only 40 intersect the actual mouth band; emoji 744, only 50 intersect. Current 512 cap counts unrelated edges before the relevance predicate. | Bound the input scan by the existing point budget and actual constructed mouth work separately; retain the 512 active-mouth cap and every slope/exterior/roof guard. |
| LEGO | Text perimeter 563 edges; emoji 543. Current code emits one strip and optional reentrant sector per edge, then refuses above 512 regardless of actual solid complexity. | Replace raw edge count with bounded library geometry construction/evaluation work. No radius, tolerance, defaults, or final wall guards may be weakened. Exact signed-profile and resource probes are required before acceptance. |

Evidence: `../../../../evidence/baseline/summary.json` (40 native records),
`../../../../inputs/corpus/manifest.json` (state/ARCH/APRQ hashes),
`../../../../inputs/preimages.json` (read-only root source preimages).
Expected refusals are not feature passes. The above directions are not yet
qualification of a corrected implementation.

Implementation update: native targeted replay now constructs both formerly
blocked strap/chim models. The final union's residual at the nominal roof
contains a positive recessed void plus coincident contact facets down to
Z=7.6. Its bbox is therefore not the nearest genuine void boundary. A second
sufficient proof uses horizontal levels read from final mesh faces (9.2 for
these models), verifies the entire footprint prism to measured/interior levels,
and retains any nonzero residual's bounding box as a conservative missing-Z
bound. There is no small-volume acceptance threshold. Query-only source and residual
simplification each receive half the existing 1e-7 mm maximum budget; the full
budget is deducted from the clearance. Manufacturing meshes are not simplified
by this proof. All 22 R2 roof/floor regressions and near-boundary controls pass
native, including failed after-bevel roofs, detached roofs/floors and 1e-6 mm
positive skins. This is not a claim that arbitrary source topology is resolved.

The parent production runtime was copied into this room before recapture:
MJS `950225880c23476f9a6523111ac5a33aebb24f1d89bfa1d8f60bff1b1cc1c375`,
WASM `211b392f097eb55c8b52898e7b6123cb4b4b4539bfc68a5223d2f9e4c4d0e4cd`.
Matching JS dependency closure is pinned in `inputs/production-runtime-pins.json`.
All 120 recaptured state/ARCH/APRQ files match the original capture byte-for-byte
(`reports/production-recapture.json`). This document records the early analysis;
the final checked results and remaining limits are in `ACCEPTANCE.json` and the
room's immutable `reports/HANDOFF.md` when the release manifest is published.

Integration ownership: three child source files only:
`source-assembly/src/source_assembly.cpp`, `mechanics/src/mechanics.cpp`,
`mechanics/src/tunnel_guard.cpp.inc`. Existing headers, layouts, ABI versions,
semantics getters, catalog values, final export gates and root registry remain
the preimages. `native/product-bridge.cpp` is an unchanged private build input,
not a proposed root change. No new parent construction API or Rust layout is
needed. The immutable manifest will identify the exact preimages and replacements.

Pinned Manifold headers explicitly state that WithContext is consumed by
Status for deferred CSG and certain eager operations. Volume, BoundingBox and
GetMeshGL do not observe it. Candidate work must evaluate through Status with
context first and use checkpoints between bounded operations. A single large
boolean is not promised mid-operation interruption.
