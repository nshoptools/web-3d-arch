# Baseline actual UI obstructions (ongoing implementation acceptance)

Source prefix for every location below: `tmp/reviews/codex/runs/20260908-product-release-builder/work/production-owned-r2-1/inputs/source/`. These are exact prepared bytes, not a claim about subsequently edited main. Release1147ad94ad005ffdb5f2524e4d9e6ada71778f3cc84c1b3cf590b2165c3b621f / prepared136b8b90a35e4007a6e110db7a2737d73eb25f12c8b33fc9b44b595fb333d2e9 independently fully verified before pilot. No product edits.

1. **Invalid rescue package during parameter commit.** Actual Chromium UI create/import SVG, set size, immediately download rescue. ZIP declares complete:true/issues:[] but selected manifest missing. Evidence ownrun `evidence/campaign-r1-chromium/svg-after.arch-project.zip`, SHA d2fb8df8efa5a28d3813fdc822472ea49ce6e700a93db97c69308e19f258ab15; bounded full inventory `evidence/campaign-r1-download-readback.jsonl`; screenshot/DOM `campaign-r1-chromium/FLOW-SVG.*`.
   Source: `src/storage/store.mjs:351` captures head; :356 captures inventory hashes; :388 calls a fresh #load; :389 selects that load's hash without adding it to the captured inventory. `src/storage/package.mjs:16` publishes that selected hash and complete flag; `src/app/jobs.mjs:102` exposes rescue while edits can occur. This code supports a cross-generation snapshot race; no exact scheduler/physical-crash proof claimed. Fix candidate should select/verify one captured generation or retry under a coherent snapshot, while retaining rescue after expiry.

2. **Settings UI roundtrip cannot finish.** Actual UI JSON import and reset succeed through HTTPS/real SQLite; account B does not receive A profile/source. Export JSON stays disabled with “Nhân chưa đăng ký đường xuất JSON cài đặt trong bản dựng này.” Chromium and WebKit agree. Source `src/ui/account/SettingsDialog.tsx:264` gates on registered export format, :268 is the button. Evidence `campaign-r2-{chromium,webkit}/SETTINGS-EXPORT-BLOCKED.{png,dom.json,ui.txt}` and `SETTINGS-NOPROJECT-result.json`. This is a composition/contract absence, not a disabled-button styling issue; dedicated profile UI is also absent in this baseline.

3. **Text-as-source UI has no adoption.** Real Be Vietnam Pro selection, exact NFD “Việt Nam”, then “Dùng chữ làm hình”: public revision advances and rescue readback has text.asSource:true but source absent. `src/ui/text/TextControls.tsx:313` sends text.update; `src/app/projects.mjs:128` only changes the text record. Evidence `focused-r2-chromium/text-toggle.arch-project.zip`, `NFD-AS-SOURCE-UI-{result.json,ui.txt,png}`, repeated focused-r3. Explicit .txt import is a separate path and returned PRODUCT_ADOPTION_BLOCKED at `src/integration/product-source-contexts.mjs:430`; its raw refusal is preserved, not counted as supported text manufacturing.

Additional open observed paths: default keycap confirmation40→41.25→41.384369mm (r2 Chromium/WebKit screenshots); colored Noto COLRv1 adoption and explicit conversion completed in focused-r2, original hash retained, then build still refused PRODUCT_SOURCE_CONVERSION_REQUIRED. Not qualifying the known curved feature set from refusals.

Firefox startup control: `evidence/ff-navigation-r1-firefox/navigation.json`: same actual host/context/cert, initial persistent page stays about:blank for10s; explicit newPage health request200 in3929ms. Host itself verified public inventory in36.61s; browser launch10.73s in focused-r1. Production TLS/COI untouched; reusable runtime now creates a fresh tab and logs startup phases. One control does not prove underlying browser cause or universal startup bound.

The source SHA table is `TOP-FAILURES-source-pins.json`. Full acceptance remains not-ready; running followups are not erased by this summary.
